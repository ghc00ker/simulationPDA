; 括号匹配 PDA (Deterministic)
; 接受语言: 正确匹配的括号串，包括空串
; 例如: "", "()", "(())", "()()", "(()())"

; 状态集
#Q = {q0}

; 输入符号集（左括号、右括号）
#S = {(,)}

; 栈符号集（X表示一个左括号，z是栈底符号）
#G = {X,z}

; 初始状态
#q0 = q0

; 栈底初始符号
#z0 = z

; 终结状态集（q0既是初始状态也是终态，接受空串）
#F = {q0}

; 转移函数
; 读到左括号：压栈X
q0 ( z q0 Xz
q0 ( X q0 XX

; 读到右括号：弹栈（X被替换为空_）
q0 ) X q0 _
